# Offen

- **Source**: https://github.com/offen/offen
- **Status**: ok
- **Image**: offen/offen:latest (Docker Hub, 38k+ pulls)
- **Notes**: Fair web analytics that lets users access their data. The repo's docker-compose.yml is for development (uses build directives). Production deployment uses the offen/offen image directly with a single container and embedded SQLite.
