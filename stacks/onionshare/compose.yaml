services:
  onionshare:
    image: pataquets/onionshare:latest
    container_name: onionshare
    restart: unless-stopped
    ports:
      - "${ONIONSHARE_PORT:-17600}:17600"
    volumes:
      - shared-files:/home/onionshare/shared
    environment:
      TZ: ${TZ:-UTC}

volumes:
  shared-files:
