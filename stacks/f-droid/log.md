# Status: skipped

F-Droid (fdroidserver) is a command-line tool for managing F-Droid repositories, not a self-hosted web service. The Docker images on Docker Hub (fdroid/ci) are CI-only images for building Android apps. There is no official Docker Compose setup or production web server image suitable for self-hosting as a stack.
