services:
  karaoke-eternal:
    image: radrootllc/karaoke-eternal:latest
    container_name: karaoke-eternal
    restart: unless-stopped
    ports:
      - "${KE_PORT:-8080}:8080"
    environment:
      - TZ=${TZ}
    volumes:
      - ke_data:/config
      - ${MEDIA_PATH}:/media:ro
