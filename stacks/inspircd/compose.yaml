services:
  inspircd:
    image: inspircd/inspircd-docker:latest
    ports:
      - "6667:6667"
      - "6697:6697"
    environment:
      - INSP_NET_NAME=LocalNet
      - INSP_ENABLE_DNSBL=no
    volumes:
      - config:/inspircd/conf
    restart: unless-stopped

volumes:
  config:
