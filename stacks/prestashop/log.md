# PrestaShop Stack

## Research
- Official Docker Hub image: `prestashop/prestashop` (confirmed tags available including `latest`)
- PrestaShop uses MySQL/MariaDB as its database

## Compose
- Used official `prestashop/prestashop:latest` image
- MySQL 8.0 for database
- Standard environment variables for PrestaShop auto-install configuration
- All configurable values exposed via .env file
