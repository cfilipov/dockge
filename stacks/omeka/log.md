# Omeka Classic

Omeka Classic doesn't have its own Docker image separate from Omeka S.
Used `omeka/omeka-s:latest` as the closest available official image.
The original Omeka Classic repo (github.com/omeka/Omeka) has no Docker support.
Added MariaDB as the database backend.
