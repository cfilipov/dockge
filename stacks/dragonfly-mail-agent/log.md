# DragonFly Mail Agent (DMA)

## Status: SKIPPED

## Research
- GitHub repo: https://github.com/corecode/dma — no Docker files or references
- Docker Hub search: no DMA-specific images found

## Reason for Skip
DMA is a lightweight local mail transfer agent designed for system installation (C source, Makefile). No Docker image exists. It's a CLI-only tool typically installed as a system sendmail replacement.
