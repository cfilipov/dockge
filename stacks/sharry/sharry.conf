sharry.restserver {
  base-url = "http://localhost:9090"
  bind {
    address = "0.0.0.0"
    port = 9090
  }
  backend {
    jdbc {
      url = "jdbc:postgresql://sharry-db:5432/sharry"
      user = "sharry"
      password = "changeme_db_pass"
    }
    signup {
      mode = "open"
    }
  }
}
