services:
  kavita:
    image: jvmilazz0/kavita:latest
    container_name: kavita
    environment:
      - TZ=Etc/UTC
    volumes:
      - ./config:/kavita/config
      - ./manga:/manga
    ports:
      - "5000:5000"
    restart: unless-stopped
