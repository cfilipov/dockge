# Bolt CMS

- **Source**: https://github.com/bolt/core
- **Image**: php:8.2-fpm + nginx:alpine + mariadb:10
- **Notes**: Based on the official compose.yml in the Bolt repo (branch 6.0). No pre-built Docker image; uses PHP-FPM + Nginx + MariaDB. The repo's compose.yml also includes PHPMyAdmin and Node for asset building, simplified here.
