services:
  komga:
    image: gotson/komga:latest
    container_name: komga
    user: "1000:1000"
    environment:
      - TZ=Etc/UTC
    volumes:
      - ./config:/config
      - ./data:/data
    ports:
      - "25600:25600"
    restart: unless-stopped
