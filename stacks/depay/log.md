# DePay

- **Source**: https://github.com/DePayFi
- **Status**: skipped
- **Notes**: DePay is a Web3 payment protocol/widget library (JavaScript SDK). It is not a self-hosted application and has no official Docker image or docker-compose file. The project provides client-side payment widgets that integrate into existing web apps, not a standalone server to self-host. No Docker Hub results match an official DePay image.
