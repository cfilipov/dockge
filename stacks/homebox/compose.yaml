services:
  homebox:
    image: ghcr.io/sysadminsmedia/homebox:latest
    container_name: homebox
    restart: unless-stopped
    environment:
      - TZ=UTC
    ports:
      - "3100:7745"
    volumes:
      - homebox-data:/data

volumes:
  homebox-data:
