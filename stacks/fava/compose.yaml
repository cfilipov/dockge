services:
  fava:
    image: yegle/fava:latest
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    volumes:
      - fava-data:/bean
    environment:
      - BEANCOUNT_FILE=/bean/${BEANCOUNT_FILE}

volumes:
  fava-data:
