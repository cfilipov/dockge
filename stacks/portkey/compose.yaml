services:
  portkey:
    image: ghcr.io/moves-rwth/portkey:latest
    container_name: portkey
    ports:
      - "${PORTKEY_PORT:-8080}:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
