services:
  fredy:
    image: ghcr.io/orangecoding/fredy:master
    container_name: fredy
    ports:
      - "9998:9998"
    volumes:
      - ./conf:/conf
      - ./db:/db
    restart: unless-stopped
