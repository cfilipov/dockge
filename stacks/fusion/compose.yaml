services:
  fusion:
    image: ghcr.io/0x2e/fusion:${FUSION_TAG:-latest}
    container_name: fusion
    restart: unless-stopped
    ports:
      - "${FUSION_PORT:-8080}:8080"
    environment:
      - FUSION_PASSWORD=${FUSION_PASSWORD}
    volumes:
      - fusion_data:/data

volumes:
  fusion_data:
