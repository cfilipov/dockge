services:
  ergo:
    image: ghcr.io/ergochat/ergo:stable
    init: true
    ports:
      - "6667:6667/tcp"
      - "6697:6697/tcp"
    volumes:
      - data:/ircd
    restart: unless-stopped

volumes:
  data:
