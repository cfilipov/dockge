services:
  youtube-dl:
    image: nbr23/youtube-dl-server:latest
    container_name: youtube-dl-server
    restart: unless-stopped
    ports:
      - "${YDL_SERVER_PORT}:8080"
    volumes:
      - ${DOWNLOADS_DIR}:/youtube-dl
      - ./config.yml:/app_config/config.yml:ro
