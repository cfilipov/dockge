services:
  yarnd:
    image: prologic/yarnd:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      PUID: "1000"
      PGID: "1000"
    volumes:
      - ./data:/data
