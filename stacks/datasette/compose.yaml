services:
  datasette:
    image: datasetteproject/datasette:latest
    restart: unless-stopped
    ports:
      - "8001:8001"
    volumes:
      - datasette-data:/data
    command: ["datasette", "serve", "/data", "--host", "0.0.0.0", "--port", "8001"]

volumes:
  datasette-data:
