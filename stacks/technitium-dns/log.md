# Technitium DNS Server — Research Log

## Sources checked
1. `docker-compose.yml` in repo root (`https://raw.githubusercontent.com/TechnitiumSoftware/DnsServer/master/docker-compose.yml`) — found complete compose file

## Compose file origin
Taken directly from the official repository's `docker-compose.yml`.

## Modifications
- Removed `version:` field if present (already absent — Compose V2 format)
- No other modifications; file used as-is from the repo
