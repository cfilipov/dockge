# Payload CMS

Payload CMS has no official Docker image on Docker Hub or GHCR.
It's a Next.js-native framework installed via `npx create-payload-app`.
Used node:22-alpine as a generic runtime with MongoDB backend.
No official docker-compose exists in the repo.
