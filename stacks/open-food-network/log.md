# Open Food Network

- **Source**: https://github.com/openfoodfoundation/openfoodnetwork
- **Status**: skipped
- **Reason**: The project has a Dockerfile for development but no published Docker image on any registry. The docker-compose.yml uses `build:` directives only and cannot be converted to image-based deployment without a published image.
