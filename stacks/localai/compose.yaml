services:
  api:
    image: quay.io/go-skynet/local-ai:master
    ports:
      - "8080:8080"
    environment:
      - MODELS_PATH=/models
    volumes:
      - models:/models
      - images:/tmp/generated/images/
    restart: unless-stopped

volumes:
  models:
  images:
