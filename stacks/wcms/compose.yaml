services:
  wcms:
    image: php:${PHP_VERSION:-8.3-apache}
    container_name: wcms
    restart: unless-stopped
    ports:
      - "${WCMS_PORT:-8080}:80"
    volumes:
      - wcms-data:/var/www/html
    environment:
      - TZ=${TZ:-UTC}

volumes:
  wcms-data:
