# Azimutt Stack

- Based on official docker-compose.yml from azimuttapp/azimutt repo
- Replaced `build:` with `ghcr.io/azimuttapp/azimutt:latest` image
- PostgreSQL 16 as database backend
- Backend on port 4000
- Environment variables for DB credentials and Phoenix secret key
