services:
  app:
    image: ghcr.io/cmintey/wishlist:latest
    restart: unless-stopped
    ports:
      - "${WISHLIST_PORT:-3280}:3280"
    volumes:
      - ./uploads:/usr/src/app/uploads
      - ./data:/usr/src/app/data
    env_file:
      - .env
