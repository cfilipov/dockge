# Django-CRM

- **Source**: https://github.com/MicroPyramid/Django-CRM
- **Status**: skipped
- **Reason**: The project has a docker-compose.yml but all app services use `build:` directives. No published Docker image exists on any registry. The compose file cannot be converted to an image-based deployment without a published image.
