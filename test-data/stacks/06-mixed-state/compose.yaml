services:
  app:
    image: nginx:latest
    restart: unless-stopped
    ports:
      - "8080:80"
  worker:
    image: python:3.12-slim
    command: python worker.py
    restart: unless-stopped
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: secret
